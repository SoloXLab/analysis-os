# Diagrams

This folder holds visual references for the Analysis OS reasoning process itself (not analysis outputs — those live in `examples/`).

Recommended diagrams to add here as the project matures:
- `analysis-loop.svg` — the six-phase loop with the feedback path (see `docs/analysis-loop.md` for the ASCII version)
- `reasoning-graph.svg` — node/edge types from `docs/graph-model.md`
- `dependency-graph-example.svg` — a worked example dependency graph
- `root-cause-tree-example.svg` — a worked example root-cause tree

For now, use the Mermaid snippets embedded in `.claude/skills/analysis-os/prompts/dependency-graph.md` and render them with any Mermaid-compatible tool (GitHub renders Mermaid natively in Markdown).
